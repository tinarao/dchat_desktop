export type UserKebabCase = {
    id: number
    name: string
    inserted_at: string
    updated_at: string
}

export type User = {
    id: number
    name: string
    insertedAt: string
    updatedAt: string
}
