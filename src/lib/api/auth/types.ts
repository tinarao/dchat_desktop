export type UserKebabCase = {
    id: number
    name: string
    public_key?: string
    inserted_at: string
    updated_at: string
}

export type User = {
    id: number
    name: string
    publicKey?: string
    insertedAt: string
    updatedAt: string
}
